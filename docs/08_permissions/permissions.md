# 权限与安全（V1）

目标阶段不做复杂权限系统，但需满足基本安全基线：

- 运行安全：Codex CLI 仅在允许的工作目录下执行；默认限制为配置的 root/cwd 白名单。
- 指令范围：仅支持 Codex 相关命令；拒绝其他系统命令透传。
- 渠道控制：只响应已配置的 Bot Token 与允许的 guild/thread；可配置允许名单。
- 并发控制：同一 session 互斥；超时后自动释放锁；支持 /cancel。
- 日志脱敏：过滤 token、路径等敏感信息；错误回显不包含机密配置。
- 审计留痕（轻量）：保留执行摘要与时间戳，便于排查；可选落盘/持久化。
